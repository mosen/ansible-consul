---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: vmware_desktop
lint:
  name: yamllint
platforms:
  - name: instance
    box: centos/7
provisioner:
  name: ansible
  inventory:
    hosts:
      consul_instances:
        hosts:
          instance:
        vars:
          consul_encrypt_enable: true
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
